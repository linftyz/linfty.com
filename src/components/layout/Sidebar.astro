---
import site from "@/config/site";
import Nav from "./Nav.astro";
import ThemeToggle from "./ThemeToggle.astro";
import { Icon } from "astro-icon/components";
import Brand from "./Brand.astro";
import SearchModal from "../blocks/SearchModal.astro";
import SearchBox from "./SearchBox.astro";
---

<aside
  class="flex w-full flex-col border-b border-border bg-muted/20 p-5 md:h-full md:min-h-0 md:w-64 md:flex-none md:border-r md:border-b-0 md:p-8 lg:w-80 lg:p-10 xl:w-[22rem]"
  data-pagefind-ignore="all"
>
  <Brand />

  <SearchBox />

  <Nav />

  <div class="mt-5 flex items-center justify-between md:mt-auto md:pt-6">
    <div class="flex gap-4 text-muted-foreground/70">
      {
        site.social.map((item) => (
          <a
            href={item.href}
            class="cursor-pointer transition-colors hover:text-primary"
            aria-label={item.name}
          >
            <Icon name={item.icon} size={24} />
          </a>
        ))
      }
    </div>

    <ThemeToggle />
  </div>

  <SearchModal />
</aside>
